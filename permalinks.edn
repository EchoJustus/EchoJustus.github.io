;; Generated by clogem-press. Committed deliberately (DESIGN.md §7.2):
;;   - whole-site permalink collision detection without re-reading every file
;;   - a tombstone ledger so retired URLs can emit redirect stubs (D-15)
;;   - identical URLs from a --no-write build
;; It is an AUXILIARY. Identity lives in the files' own `permalink:` front matter,
;; which is what makes `git mv` URL-safe.
{:version 1,
 :permalinks
 {"/pages/669d0b/"
  {:key "/01.Notes|1|welcome",
   :langs ["en"],
   :paths ["01.Notes/01.welcome.md"]},
  "/pages/7f90e0/"
  {:key "_posts||2026-09-05-hello-world",
   :langs ["en"],
   :paths ["_posts/2026-09-05-hello-world.md"]}},
 :tombstones {}}
